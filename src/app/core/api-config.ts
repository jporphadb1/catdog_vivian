/**
 * Ad Service base URL. Plain constant instead of Angular's environment-file
 * machinery — a single overridable value is all this stage needs.
 */
export const AD_SERVICE_BASE_URL = 'http://localhost:8081';
